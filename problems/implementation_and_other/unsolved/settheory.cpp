#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t,n,temp,i,j;
    int count;
    vector<int>a;
    //vector<int>ans;
    //vector<int>buffer;
    cin >> t;
    while(t>0)
    {
        cin >> n;
        
        for(i=0;i<n;i++)
        {
            cin >> temp;
            a.push_back(temp);
        }
/*
        buffer.push_back(a[i]+1);
        ans.push_back(1);
        count=1;

        temp=2;
        while(count!=n)
        {
            for(i=0;i<n;i++)
            {
                if()
            }
        }
*/
        a.clear();
        ans.clear();
        t--;
    }
    return 0;
}